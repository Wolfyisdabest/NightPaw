[build-system]
requires = ["maturin>=1.7,<2"]
build-backend = "maturin"

[project]
name = "nightpaw_rs"
version = "0.1.0"
requires-python = ">=3.14"

[tool.maturin]
bindings = "pyo3"
module-name = "nightpaw_rs"
